#include <iostream>
#include <string>
#include <bits/stdc++.h> 
using namespace std;

bool isUpdated;

string getPlayerID()
{ 
// 获取并返回可以识别玩家身份的id
return "encodedString";
}

const string playerID=getPlayerID(); 
int update()
{
// get id 到服务器并下载更新
}
int main() 
{
	cout << "Checking for update";
	update();
	if (isUpdated)
		{
		return 0;		
		}
	else 
		{
		system("./spring-engine-linux-masterbuild/spring --menu 'Chobby $VERSION'"); 

		}
    	return 0;
}
